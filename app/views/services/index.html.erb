<h1 id="main-title">Fancy anything? 🤩</h1>
<div class="container">
  <div class="cards">
      <% @services.each do |service| %>
        <%= link_to service_path(service) do %>
          <div class="card-event">
            <% if service.image.nil? %>
              <img src="https://images.unsplash.com/photo-1540039155733-5bb30b53aa14?ixlib=rb-1.2.1&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=2148&q=80" />
            <% else %>
              <img src="<%= service.image %>" alt="">
            <% end %>
            <div class="card-event-infos p-3 pt-4">
              <h2><%= service.name %></h2>
              <h3 class="pb-3"><%= number_with_delimiter(service.price, delimiter: ' ') %> €</h3>
                <p class="text-ellipsis--2 p-0"><%= service.description %></p>
            </div>
          </div>
        <% end %>
      <% end %>
  </div>
</div>
